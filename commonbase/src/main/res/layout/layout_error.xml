<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tool="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:gravity="center_horizontal"
    android:orientation="vertical">


    <ImageView
        android:id="@+id/iv_error"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tool:src="@mipmap/empty_network_error"/>

    <TextView
        android:id="@+id/tv_error_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:gravity="center"
        android:lineSpacingExtra="2dp"
        android:maxWidth="200dp"
        android:textColor="@color/multi_state_msg_color"
        android:textSize="12dp"
        tool:text="网络好像出问题了..." />

    <com.xingxiu.frame.view.roundView.RoundTextView
        android:id="@+id/tv_retry"
        android:layout_width="128dp"
        android:layout_height="40dp"
        android:gravity="center"
        android:includeFontPadding="false"
        android:text="刷新"
        android:textColor="@color/multi_state_retry_text_color"
        android:textSize="14dp"
        app:rv_backColor="@color/transparent"
        app:rv_cornerRadius="10dp"
        app:rv_strokeColor="@color/multi_state_retry_stroke_color"
        app:rv_strokeWidth="1dp" />

</LinearLayout>